{
  "name": "wsx-hono-example",
  "private": true,
  "scripts": {
    "dev": "bun run wrangler dev --assets ./public",
    "deploy": "bun run wrangler deploy --minify"
  },
  "dependencies": {
    "@wsx/hono": "workspace:*",
    "@wsx/core": "workspace:*",
    "hono": "^4.8.4"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
